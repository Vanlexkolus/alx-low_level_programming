#include"main.h"
/**
 * main - this code prints alphabets in small letters
 *
 * Return: always return zero
 */

int main(void)
{
	int u = 'a';
	int v = 'z';
		if (u <= z)
		{
			putchar ('u');
			u++;
		}

	return (0);
}
