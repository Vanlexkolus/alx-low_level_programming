#include <stdio.h>
/**
 * main - This is void
 *
 * Return: always return zero for this code
 */

int main(void)
{
	char X = '0';

	while (X <= '9')
	{
		putchar(X);
		X++;
	}
	putchar('\n');
	return (0);
}
