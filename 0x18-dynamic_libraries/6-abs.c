#include "main.h"
/**
 * _abs - this function cal the absolute value of an integer
 * @g: g collect the data
 * Return: return zero
 */

int _abs(int g)
{
	if (g >= 0)
	{
		return (g);
	}
	else
	{
		return (-g);
	}
	return (0);
}
